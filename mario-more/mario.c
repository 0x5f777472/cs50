#include <cs50.h>
#include <stdio.h>

int main(void)
{
    while (True) {
        int input = get_int("Height: ");
        if (input < 1 || input > 8) {
            break;
        }
        for (int i = 1; i <= input; i++) {
            for (int j = ) {

            }
        }
    }

}